Can be compiled with `pdfLaTex`. Chapters 01, 02, 03 have `% !TEX root = classicthesis.tex` added to allow for compilation from any of those files, although it is reccomended to compile from `classicthesis.tex`. A compiled PDF is available at `classicthesis.pdf`. 
